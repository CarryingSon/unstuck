# OpenAI API key used by the local Express API server.
OPENAI_API_KEY="sk-..."

# Optional: default model for task planning.
OPENAI_MODEL="gpt-5-mini"

# Optional: local API port used by both server and Vite proxy.
API_PORT="8787"
